<%= form_for(@picture_tag) do |f| %>
  <% if @picture_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@picture_tag.errors.count, "error") %> prohibited this picture_tag from being saved:</h2>

      <ul>
      <% @picture_tag.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
