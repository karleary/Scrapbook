<%= form_for(@tag_picture) do |f| %>
  <% if @tag_picture.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tag_picture.errors.count, "error") %> prohibited this tag_picture from being saved:</h2>

      <ul>
      <% @tag_picture.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tag_id %><br />
    <%= f.number_field :tag_id %>
  </div>
  <div class="field">
    <%= f.label :picture_id %><br />
    <%= f.number_field :picture_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
